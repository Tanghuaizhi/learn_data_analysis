Lesson 4
========================================================

***

### Scatterplots and Perceived Audience Size
Notes:预测人数和实际人数的关系，如果猜的准，应落在45°线上。通过两者关系图，可以看出一些猜和实际的规律。

***

### Scatterplots
Notes:注意qplot可以不用指定x、y，函数会自动按x第一、y第二识别。
散点图生成后，通常，最好想一下是什么产生了数据集中的某些怪现象或值。这能帮助我们解释脏数据。



```{r Scatterplots}
getwd()
dir()

library(ggplot2)
pf <- read.csv('pseudo_facebook.tsv', sep = '\t')

qplot(x = age, y = friend_count, data = pf)
```

***

#### What are some things that you notice right away?
Response:从图发现，年龄集中在30岁以下，好友人数集中在1000人以下甚至更少。

参考：年轻人的好友数会多达好几千人；有明显的虚假年龄。

***

### ggplot Syntax
Notes:qplot是快速绘图。ggplot是更正式的语法，主要的差别在于：一是需要指定geom或者图表类型；二是需要用aes包裹。
另外需要注意的是，可以通过“+”运算，来逐步增加图层并随时调整修改图像。


```{r ggplot Syntax}
qplot(x = age, y = friend_count, data = pf)
ggplot(aes(x = age, y = friend_count), data = pf) + geom_point() +
  xlim(13, 90)
summary(pf$age)
```

***

### Overplotting
Notes:重叠绘制的情况出现时，图像会看不清晰。这时，可以用α和geom来设置透明度解决。
继续用geom_jitter()可以增加抖动噪音，让重复的点分离开来。

```{r Overplotting}
ggplot(aes(x = age, y = friend_count), data = pf) + geom_jitter(alpha = 1/20) +
  xlim(13, 90)
```

#### What do you notice in the plot?
Response:1、好友人数呈现“增加-减少-增加-再减少”的趋势；
2、年轻人好友人数更多；
3、号称年龄70岁的人数比较多。

答案：年轻用户人数并没有想像的那么高，大概在1000以下；其次是69岁人数比较多。

***

### Coord_trans()
Notes:用coord_trans来调整坐标轴的显示。
运行代码 ?coord_trans 以查找有关 coord_trans() 的文档，或者访问 http://docs.ggplot2.org/current/coord_trans.html


```{r Coord_trans()}
d <- subset(pf, friend_count > 0)
ggplot(aes(x = age, y = friend_count), data = d) + 
  geom_jitter(alpha = 1/20) +
  xlim(13, 90) + 
  coord_trans(y = "sqrt")

ggplot(data = pf, aes(x = age, y = friend_count)) +
  geom_point(alpha = 1/20, position = position_jitter(h = 0)) +
  xlim(13, 90) + 
  coord_trans(y = 'sqrt')
```

#### Look up the documentation for coord_trans() and add a layer to the plot that transforms friend_count using the square root function. Create your plot!

由于抖动jitter会产生一些负数，可以考虑用position来处理。

如果是原本数据中的负数和0数据，经尝试可用subset来处理。


```{r}
ggplot(aes(x = age, y = friend_count), data = pf) + 
  geom_point(alpha = 1/20, position = position_jitter(h = 0)) +
  xlim(13, 90) + 
  coord_trans(y = "sqrt")
```

#### What do you notice?
与之前相比，该图的变化趋势更明显。

***

### Alpha and Jitter
Notes:探索friends initiated 和 age的关系

```{r Alpha and Jitter}
ggplot(aes(x = age, y = friendships_initiated), data = pf) +
  geom_point(alpha = 1/50, position = position_jitter(h = 0)) +
  coord_trans(y = 'sqrt')

```

***

### Overplotting and Domain Knowledge
Notes:
转换坐标轴，让数据更有意义
***

### Conditional Means
Notes:课程链接中有关于dplyr包的简介及教程等

常用的函数包括：filter()、group_by()、mutate()、arrange()

```{r Conditional Means}
library(dplyr)

age_group <- group_by(pf, age)
pf.fc_by_age = summarise(age_group,
          friend_count_mean = mean(friend_count),
          friend_coun_median = median(friend_count),
          n = n())

pf.fc_by_age <- arrange(pf.fc_by_age, age)

head(pf.fc_by_age)
```

上述代码的另一种写法
```{r}
pf.fc_by_age <- pf %>%
  group_by(age) %>%
  summarise(friend_count_mean = mean(friend_count),
            friend_coun_median = median(friend_count),
            n = n()) %>%
  arrange(age)

head(pf.fc_by_age, 20)
```


Create your plot!

```{r Conditional Means Plot}

ggplot(data = pf.fc_by_age, aes(x = age, y = friend_count_mean)) +
  geom_line()
```

***

### Overlaying Summaries with Raw Data
Notes:
ggplot允许我们轻松创建数据的各种汇总，十分适合快速探索并叠加到原始数据图形
叠加的图，可以传递参数stat，并用fun.y给y轴设置一个函数
可以添加多个叠加来辅助可视化

注意：合理使用coord_cartesian()来放大图像，但注意要去掉coord_trans

```{r Overlaying Summaries with Raw Data}
ggplot(data = pf, aes(x = age, y = friend_count)) +
  xlim = c(13, 90) + 
  geom_point(alpha = 1/20, 
             position = position_jitter(h = 0),
             color = 'orange') +
  coord_trans(y = 'sqrt') +
  geom_line(stat = 'summary', fun.y = mean) + 
  geom_line(stat = 'summary', fun.y = quantile, fun.args = list(probs = .1),
            linetype = 2, color = 'blue') + 
  geom_line(stat = 'summary', fun.y = quantile, fun.args = list(probs = .5),
            linetype = 1, color = 'blue') +
  geom_line(stat = 'summary', fun.y = quantile, fun.args = list(probs = .9),
            linetype = 2, color = 'blue')


ggplot(data = pf, aes(x = age, y = friend_count)) +
  coord_cartesian(xlim = c(13, 70), ylim = c(0, 1000)) + 
  geom_point(alpha = 1/20, 
             position = position_jitter(h = 0),
             color = 'orange') +
  geom_line(stat = 'summary', fun.y = mean) + 
  geom_line(stat = 'summary', fun.y = quantile, fun.args = list(probs = .1),
            linetype = 2, color = 'blue') + 
  geom_line(stat = 'summary', fun.y = quantile, fun.args = list(probs = .5),
            linetype = 1, color = 'blue') +
  geom_line(stat = 'summary', fun.y = quantile, fun.args = list(probs = .9),
            linetype = 2, color = 'blue')

```

#### What are some of your observations of the plot?
Response:1、所有年龄的好友平均数大于中位数，即少部分人拥有数量较高的朋友数量；2、30-68岁的好友数量较为稳定;3、1000好友数是很少出现的，70随以后的好友数量逐渐增加

***

### Moira: Histogram Summary and Scatterplot
See the Instructor Notes of this video to download Moira's paper on perceived audience size and to see the final plot.

Notes:用摘要的组合图，能更直观的显示统计汇总结果

***

### Correlation
Notes:
可以使用with()函数

```{r Correlation}
cor.test(x = pf$age, y = pf$friend_count, method = 'pearson')

with(pf, cor.test(age, friend_count, method = 'pearson'))
```

Look up the documentation for the cor.test function.

What's the correlation between age and friend count? Round to three decimal places.
Response:0.0274

***

### Correlation on Subsets
Notes:cor.test的默认方法就是perason相关系数

```{r Correlation on Subsets}
with(subset(pf, age > 13 & age <=70), cor.test(age, friend_count, method = 'pearson'))
```

***

### Correlation Methods
Notes:不同的方法，相关系数不一样。同时，虽然相关系数的值很有用，但是无法取代前面课程的散点图和摘要

***

## Create Scatterplots
Notes:
尽可能使用ggplot
基本的框架是三个：数据、aes装饰、数据显示方式。
再然后就是对图的修饰，比如坐标轴、区间等。

```{r}
ggplot(data = pf, aes(x = www_likes_received, y = likes_received)) + 
  geom_point() 

```

***

### Strong Correlations
Notes:
可以从上图看到，有一些跳跃的异常值，因此需要确定好x、y的limit。可以用quantile命令，查看95%的数据。
找到最合适的拟合直线，用平滑器添加到图中。
然后再看看相关系数。
```{r Strong Correlations}
ggplot(data = pf, aes(x = www_likes_received, y = likes_received)) + 
  geom_point() +
  xlim(0, quantile(pf$www_likes_received, 0.95)) +
  ylim(0, quantile(pf$likes_received, 0.95)) + 
  geom_smooth(method = "lm", color = 'red')
```

What's the correlation betwen the two variables? Include the top 5% of values for the variable in the calculation and round to 3 decimal places.

相关系数在 X 或 Y 的线性转换下是不变的，并且当 X 和 Y 都被转换为 z 分数时，回归线的斜率就是相关系数。
值得注意的是，我们并不总是对大部分数据感兴趣。有时候，我们关注的是异常值，而且重要的是，我们要了解它们的值以及它们为何出现在数据集中。


```{r Correlation Calcuation}
with(pf, cor.test(x = www_likes_received, y = likes_received, method = 'pearson'))

```

Response:相关系数为0.948，实际中是不可能出现这样的强相关的，其中一个是另一个的超子集。


***

### Moira on Correlation
Notes:
强相关性并非就一定好，因此要首先观察相关性，看看两个变量的关系。
方法上除了观察散点图，就是计算r值。
相关系数十分重要，因为通常数据都是有相关的。尤其是，描述的事情是同一类型。比如，当做某种回归（即进行建模），要选择合适的变量放入回归，因为回归的假设就是这些变量彼此独立。需要首先计算相关性，然后确认哪些变量不要一起投入或是保留哪些变量。

***

### More Caution with Correlation
Notes:
单纯的考虑r很容易出现错误。最好是作图，发现深层次的信息。

```{r More Caution With Correlation}
install.packages('alr3')
library(alr3)
data(Mitchell)
```

Create your plot!

```{r Temp vs Month}
ggplot(data = Mitchell, aes(x = Month, y = Temp)) +
  geom_point()
```

***

### Noisy Scatterplots
a. Take a guess for the correlation coefficient for the scatterplot.

猜是0，但结果肯定不是


b. What is the actual correlation of the two variables?
(Round to the thousandths place)

0.057

```{r Noisy Scatterplots}
cor.test(Mitchell$Month, Mitchell$Temp, method = 'pearson')
```

***

### Making Sense of Data
Notes:
上述数据看似不相关，但是用月份来展示可能不正确，需要理解数据，更换合适的方案来呈现数据

解决方案是用离散坐标轴


```{r Making Sense of Data}
ggplot(data = Mitchell, aes(x = Month, y = Temp)) +
  geom_bar()

range(Mitchell$Month)

ggplot(data = Mitchell, aes(x = Month, y = Temp)) +
  geom_point() +
  scale_x_continuous(breaks = seq(0, 203, 12))

```

***

### A New Perspective

What do you notice?
Response:一年内，温度先上升后下降

The cor and cor.test functions determine the strength of a linear relationship, but they may miss other relationships in the data.

Watch the solution video and check out the Instructor Notes!
Notes:
要理解该数据，你还可以将每一年的数据相互叠加，构建出一个清晰的常规正弦曲线图。你可以在代码中使用 R 的模数运算符 %% 来这样做。尝试运行以下代码！
ggplot(aes(x=(Month%%12),y=Temp),data=Mitchell)+ 
  geom_point()
  

```{r}
ggplot(aes(x=(Month%%12),y=Temp),data=Mitchell)+ 
  geom_point()
```


***

### Understanding Noise: Age to Age Months
Notes:

```{r Understanding Noise: Age to Age Months}
ggplot(aes(x = age, y = friend_count_mean), data = pf.fc_by_age) + 
  geom_line()

head(pf.fc_by_age, 10)
```

```{r}
head(pf, 10)

pf$age_with_month <- pf$age + (12 - pf$dob_month)/12
```


***

### Age with Months Means

```{r Age with Months Means}

library(dplyr)

age_with_month_group <- group_by(pf, age_with_months)
pf.fc_by_age_months = summarise(age_with_month_group,
          friend_count_mean = mean(friend_count),
          friend_coun_median = median(friend_count),
          n = n())

pf.fc_by_age_months <- arrange(pf.fc_by_age_months, age_with_month_group)

head(pf.fc_by_age_months)



pf.fc_by_age_months <- pf %>%
  group_by(age_with_month) %>%
  summarise(friend_count_mean = mean(friend_count),
            friend_coun_median = median(friend_count),
            n = n()) %>%
  arrange(age_with_month)

head(pf.fc_by_age_months, 20)
```

Programming Assignment
```{r Programming Assignment}

```

***

### Noise in Conditional Means

```{r Noise in Conditional Means}
ggplot(data = pf.fc_by_age_months, aes(x = age_with_month, y = friend_count_mean)) +
  geom_line() +
  coord_cartesian(xlim = c(13, 71), ylim = c(0, 500))
```

视频中未进行坐标轴设置，是采用subset()函数来取age为71以下的子集
***

### Smoothing Conditional Means
Notes:
先把上述两张图同时放在一起比较，用到了gridExtra包

进一步平滑数据

这样比较，有利于在分析数据过程中，选取合适的平滑，便于模型拟合

ggplot中已经内置了上述函数，即geom_smooth()

```{r Smoothing Conditional Means}
p1 <- ggplot(data = subset(pf.fc_by_age, age < 71),
             aes(x = age, y = friend_count_mean)) +
  geom_line() +
  geom_smooth()

p2 <- ggplot(data = subset(pf.fc_by_age_months, age_with_month < 71),
             aes(x = age_with_month, y = friend_count_mean)) +
  geom_line() +
  geom_smooth()

p3 <- ggplot(data = subset(pf, age < 71),
             aes(x = round(age / 5) * 5, y = friend_count)) +
  geom_line(stat = 'summary', fun.y = mean)

library(gridExtra)
grid.arrange(p2, p1, p3, ncol =1)

```

***

### Which Plot to Choose?
Notes:
如何选择可视化图形？答案是：不用选择。
在EDA中，通常创建相同数据的多个可视化及汇总结果，从不同角度观察数据，获得信息。
***

### Analyzing Two Variables
Reflection:

一是散点图很重要，是发现信息的重要来源。在绘制过程中，有些小技巧可以让散点图可视化的时候更加清晰，如透明、增加抖动等。
二是相关性很重要，用于度量两个独立变量相互关系。能够量化两个变量的关系，但也要注意r并不代表一切，不同的数据选择方式，r值会大不一样，需要避免被r值“欺骗”。
三是可视化很重要，用于表达展示重要的信息。如将摘要与散点图叠加，直方图与散点图并列显示，平滑变量与散点图叠加等。

***

Click **KnitHTML** to see all of your hard work and to have an html
page of this lesson, your answers, and your notes!

